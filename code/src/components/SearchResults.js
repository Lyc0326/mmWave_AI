import React from 'react';
import './SearchResults.css';  // Import the CSS file

const SearchResults = () => {
    return (
        <section className="search-results-container">
            <h2>典藏品查詢結果</h2>
            <p className="no-results-message">我們有各式各樣的東東，應有盡有...</p>
        </section>
    );
};

export default SearchResults;
